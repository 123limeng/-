线性表里的元素是按线性排列的，线性表分为两大类：顺序表和链表。
1.顺序表：
  顺序表中的元素存储是连续的，内存划分的区域也是连续的。
2.链表：
  链表在物理存储上通常是非连续、非顺序的方式存储的，数据元素的逻辑顺序是通过链表中的引用体现的。
  (1).单向链表：
    内存中的对象的随机分布的，对象不但存储了相应的数据，还持有一个next引用，指向下一个对象，来
  确定一组对象的逻辑顺序。
  (2).循环链表：
    循环链表和单项链表是一样的，但是最后一个对象的next引用指向第一个对象。
  (3).双向链表：
    双向链表的对象不仅包含一个next引用指向下一个对象，还包含一个prev引用指向前一个对象。
2.栈和队列：
  栈和队列是两种比较特殊的线性表。